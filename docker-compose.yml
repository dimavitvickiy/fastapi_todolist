version: "3.4"
services:
  web:
    command: uvicorn todolist.main:app --reload --host 0.0.0.0 --port 8000
    build:
      context: .
    ports:
      - 8000:8000
    expose:
      - 8000
    networks:
      - todolist
networks:
  todolist:
